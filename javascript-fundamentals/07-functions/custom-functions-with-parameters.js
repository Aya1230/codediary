// Define a function named greet that takes one parameter: name
function greet(name) {
  // Use the parameter to log a personalized message
  console.log("Hello, " + name);
}

// Call the function and pass in "Sara" as the argument
greet("Sara"); // ✅ Output: Hello, Sara
