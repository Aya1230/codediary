function add(a, b) {
  return a + b; // Send back the result
}

let result = add(3, 5); // ✅ Store the return value (also called arguments since they're the actual values being passed)
console.log(result); // ✅ Output: 8
